'use client';

// This component is no longer in use.
// Its logic has been centralized into the AuthProvider and DashboardLayout.
// This file can be safely removed in the future.
export function ProfileSetupGuard({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
